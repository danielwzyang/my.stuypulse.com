---
interface Props {
    osis: string | undefined
    meetings: string[]
    allMeetings: string[]
    userData: {
        id_number: string
        num_checkins: number
        attendance_rate_percent: string
        num_checkouts: number
        checkout_rate_percent: string
        total_hours: number
        average_hours: number
    }
}

const { osis, meetings, allMeetings, userData } = Astro.props

import Calendar from "./Calendar.astro"
import UserData from "./UserData.astro"
import Logout from "./Logout.astro"
---

<h1 class="mb-2 text-4xl text-center">{osis}</h1>
<h1 class="text-center italic text-sm mb-5 text-[#464646]">
    attendance updated to 11/13
</h1>

<div class="flex flex-col w-full items-center mb-5 space-y-5">
    <div class="flex flex-wrap gap-5 justify-center mx-3">
        <Calendar meetings={meetings} allMeetings={allMeetings} />
        <UserData userData={userData} />
    </div>
    <Logout />
</div>
